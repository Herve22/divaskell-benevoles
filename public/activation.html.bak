<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Activation - Bénévoles</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <style>
    body {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .activation-card {
      background: white;
      border-radius: 20px;
      padding: 3rem;
      box-shadow: 0 20px 60px rgba(0,0,0,0.3);
      text-align: center;
      max-width: 500px;
      width: 90%;
      animation: slideUp 0.6s ease;
    }
    @keyframes slideUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .icon-success {
      width: 100px;
      height: 100px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 2rem;
      animation: bounce 1s ease;
    }
    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-20px); }
    }
    .icon-success i {
      font-size: 3rem;
      color: white;
    }
    .icon-error {
      width: 100px;
      height: 100px;
      background: #dc3545;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 2rem;
    }
    .icon-error i {
      font-size: 3rem;
      color: white;
    }
    .btn-login {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      border: none;
      color: white;
      padding: 1rem 2.5rem;
      border-radius: 50px;
      font-weight: 600;
      font-size: 1.1rem;
      transition: all 0.3s ease;
      margin-top: 1.5rem;
    }
    .btn-login:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
    }
    .spinner-border {
      width: 3rem;
      height: 3rem;
      border-width: 0.3rem;
    }
  </style>
</head>
<body>
  <div class="activation-card">
    <div id="loading-state">
      <div class="spinner-border text-primary" role="status">
        <span class="visually-hidden">Chargement...</span>
      </div>
      <h2 class="mt-4">Activation en cours...</h2>
      <p class="text-muted">Veuillez patienter</p>
    </div>

    <div id="success-state" style="display: none;">
      <div class="icon-success">
        <i class="bi bi-check-lg"></i>
      </div>
      <h1 class="h2 text-primary mb-3">Compte activé !</h1>
      <p class="lead text-muted mb-0">Votre compte est maintenant actif.</p>
      <p class="text-muted">Vous pouvez maintenant vous connecter.</p>
      <a href="/public/index.html" class="btn btn-login">
        <i class="bi bi-box-arrow-in-right"></i> Se connecter
      </a>
    </div>

    <div id="error-state" style="display: none;">
      <div class="icon-error">
        <i class="bi bi-x-lg"></i>
      </div>
      <h1 class="h2 text-danger mb-3">Erreur d'activation</h1>
      <p class="text-muted" id="error-message">Le lien d'activation est invalide ou a expiré.</p>
      <a href="/public/index.html" class="btn btn-login">
        <i class="bi bi-house"></i> Retour à l'accueil
      </a>
    </div>
  </div>

  <script>
    document.addEventListener("DOMContentLoaded", async () => {
      const params = new URLSearchParams(window.location.search);
      const token = params.get("token");

      console.log("=== ACTIVATION DEBUG ===");
      console.log("URL complète:", window.location.href);
      console.log("Paramètres URL:", window.location.search);
      console.log("Token extrait:", token);
      console.log("Tous les paramètres:", Object.fromEntries(params.entries()));

      if (!token) {
        console.error("❌ Aucun token trouvé dans l'URL");
        showError("Aucun token d'activation fourni.");
        return;
      }

      try {
        const url = `/api/auth/activate?token=${encodeURIComponent(token)}`;
        console.log("🔄 Appel API:", url);
        
        const res = await fetch(url);
        console.log("📡 Statut HTTP:", res.status, res.statusText);
        console.log("📡 Headers:", Object.fromEntries(res.headers.entries()));
        
        const data = await res.json();
        console.log("📦 Réponse JSON:", data);
        console.log("✅ res.ok:", res.ok);
        console.log("✅ data.message:", data.message);

        if (res.ok && data.message) {
          console.log("🎉 SUCCÈS - Affichage de l'écran de succès");
          showSuccess();
        } else {
          console.error("❌ ÉCHEC - Affichage de l'erreur");
          showError(data.error || "Erreur lors de l'activation.");
        }
      } catch (err) {
        console.error("💥 Erreur activation:", err);
        showError("Erreur réseau. Veuillez réessayer.");
      }
    });

    function showSuccess() {
      console.log("🟢 Affichage: success-state");
      document.getElementById("loading-state").style.display = "none";
      document.getElementById("success-state").style.display = "block";
    }

    function showError(message) {
      console.log("🔴 Affichage: error-state -", message);
      document.getElementById("loading-state").style.display = "none";
      document.getElementById("error-state").style.display = "block";
      document.getElementById("error-message").textContent = message;
    }
  </script>
</body>
</html>
